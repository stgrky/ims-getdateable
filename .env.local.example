NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-09-01

# Server-only. Used by `npm run seed`. Do NOT expose to the browser.
# Generate in Sanity dashboard → API → Tokens (Editor permissions).
SANITY_WRITE_TOKEN=
